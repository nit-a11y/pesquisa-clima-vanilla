# Workflow desativado - Deploy manual via SSH
# Para fazer deploy: 
# 1. ssh root@147.93.10.11
# 2. cd /var/www/sistemas/pesquisa-clima
# 3. git pull origin main
# 4. npm install --production
# 5. pm2 restart pesquisa-clima
